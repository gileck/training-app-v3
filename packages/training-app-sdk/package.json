{
  "name": "@training-app/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Client SDK for the training app. Call APIs as any user via ADMIN_API_TOKEN.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=18"
  }
}
