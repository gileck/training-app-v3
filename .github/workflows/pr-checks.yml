# PR Code Quality Checks
#
# Runs yarn checks on pull requests. Does not block merge but reports failures
# via GitHub PR comment and Telegram notification.
#
# Required GitHub Secrets:
#   - TELEGRAM_BOT_TOKEN: Your Telegram bot token
#   - OWNER_TELEGRAM_CHAT_ID: Chat ID to receive notifications

name: PR Checks

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  checks:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run checks
        id: checks
        run: |
          set +e
          OUTPUT=$(yarn checks 2>&1)
          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT

          # Save output to file for multiline handling
          echo "$OUTPUT" > checks_output.txt

          # Extract error count if present
          ERROR_COUNT=$(echo "$OUTPUT" | grep -oE '[0-9]+ error' | head -1 | grep -oE '[0-9]+' || echo "0")
          echo "error_count=$ERROR_COUNT" >> $GITHUB_OUTPUT

          echo "$OUTPUT"
          exit 0

      - name: Comment on PR (failure)
        if: steps.checks.outputs.exit_code != '0'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('checks_output.txt', 'utf8');

            // Truncate if too long
            const maxLength = 60000;
            const truncatedOutput = output.length > maxLength
              ? output.substring(0, maxLength) + '\n\n... (truncated)'
              : output;

            const body = `## ❌ Code Quality Checks Failed

            **PR:** #${{ github.event.pull_request.number }}
            **Errors:** ${{ steps.checks.outputs.error_count }}

            <details>
            <summary>Click to see full output</summary>

            \`\`\`
            ${truncatedOutput}
            \`\`\`

            </details>

            > This check is non-blocking. You can still merge, but please consider fixing these issues.
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

      - name: Comment on PR (success)
        if: steps.checks.outputs.exit_code == '0'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '## ✅ Code Quality Checks Passed\n\nAll checks completed successfully.'
            });

      - name: Send Telegram notification (failure)
        if: steps.checks.outputs.exit_code != '0'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.OWNER_TELEGRAM_CHAT_ID }}
        run: |
          MESSAGE="⚠️ *Code Checks Failed (PR #${{ github.event.pull_request.number }})*

          📝 PR: ${{ github.event.pull_request.title }}
          🔢 Errors: ${{ steps.checks.outputs.error_count }}
          👤 Author: ${{ github.actor }}"

          KEYBOARD='{"inline_keyboard":[[{"text":"📋 Open PR","url":"${{ github.server_url }}/${{ github.repository }}/pull/${{ github.event.pull_request.number }}"},{"text":"🔍 View Logs","url":"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"}]]}'

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${CHAT_ID}" \
            -d "parse_mode=Markdown" \
            -d "text=${MESSAGE}" \
            -d "reply_markup=${KEYBOARD}"
