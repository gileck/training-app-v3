/**
 * Next.js Project Configuration
 *
 * Add project-specific Next.js config here.
 * This file is NOT synced from the template - it's owned by your project.
 *
 * Example:
 *   export const nextProjectConfig = {
 *     images: {
 *       domains: ['my-cdn.com'],
 *     },
 *   };
 */

import type { NextConfig } from "next";

export const nextProjectConfig: Partial<NextConfig> = {
  // Add project-specific Next.js configuration here
};
