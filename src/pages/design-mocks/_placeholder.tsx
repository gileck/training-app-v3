/**
 * Placeholder so Turbopack can resolve the dynamic import pattern
 * `import(\`@/pages/design-mocks/${issueSlug}\`)`.
 * Real mock files only exist on PR branches.
 */
export default function Placeholder() { return null; }
