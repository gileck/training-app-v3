/**
 * Admin Sessions API Names
 *
 * API name constants for the admin user-session tracking page.
 * All endpoints are admin-only (prefixed with `admin/`).
 */

export const name = 'admin-sessions';

export const API_GET_SESSION_STATS = 'admin/sessions/getStats';
export const API_LIST_SESSION_USERS = 'admin/sessions/listUsers';
